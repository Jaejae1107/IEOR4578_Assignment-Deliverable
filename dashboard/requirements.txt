# FortuneTellers Streamlit dashboard
# Install with:  pip install -r dashboard/requirements.txt
#
# The dashboard reuses the existing ``fortunetellers/`` package, so the
# project's own dependencies (numpy, pandas, scipy, scikit-learn,
# openpyxl, holidays, neuralforecast) are also required.  We pin the
# extra UI / serialization libraries here.

streamlit>=1.32
plotly>=5.20
pandas>=2.0,<3
numpy>=1.24,<3
scipy>=1.10,<2
scikit-learn>=1.4,<2
openpyxl>=3.1,<4
holidays>=0.40
joblib>=1.3
lightgbm>=4.0

# Optional: enable the LLM polish step in app.py.  The chatbot still
# works without an API key using the deterministic regex parser.
anthropic>=0.40
