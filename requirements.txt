numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.10,<2
scikit-learn>=1.4,<2
openpyxl>=3.1,<4
holidays>=0.40

# Optional if you want native LightGBM instead of the built-in fallback.
# On macOS you may need: brew install libomp
# lightgbm>=4.0
